elixir:
  result = query!("""
    query MyQuery {
      home {
        title
        abstract
      }
    }
  """)

  page = result[:home]

header
  .wrap
    h1
      = page.title

section.wrap
  = page.abstract
